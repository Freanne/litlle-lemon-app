import React from "react";
import logo from './Logo.svg'

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Logo du site little lemon"/>
        </header>
    )
}

export default Header;